# Local-only overrides for encore run (gitignored)
# Prefer: cd api-go && ./scripts/setup-secrets-from-env.sh
# (syncs CODESIM_LIVE_AI_GEN + ANTHROPIC_API_KEY from ../api/.env)

CODESIM_LIVE_AI_GEN=1
# ANTHROPIC_API_KEY=   # optional fallback if Encore secret AnthropicApiKey empty
