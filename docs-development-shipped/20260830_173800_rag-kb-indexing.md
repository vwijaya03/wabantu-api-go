# RAG — KB Indexing

## Masalah / Kebutuhan

CRUD FAQ harus memicu indexing Pinecone secara async dengan transactional outbox.

## Perubahan

- `kb/kb.go` — tx + outbox pada Create/Update/Delete
- Pub/Sub `retrieval-index` + worker
- `POST /api/v1/knowledge-base/reindex` — backfill batch

## File utama

- `kb/kb.go`, `kb/retrieval_worker.go`, `kb/retrieval_publish.go`

## Testing

`encore test ./kb/...`

## Catatan deploy

Setelah deploy, panggil reindex per tenant staging sebelum mengaktifkan flag shadow.
